
build
# Compiled pybind11 extensions are built into the package dir
python/hylis/*.pyd
python/hylis/*.so
__pycache__/
*.py[cod]
.pytest_cache/
plans

# Benchmark corpora: large, immutable, re-downloadable (scripts/fetch_data.py)
data/

# pip install -e . metadata; regenerated on every install
*.egg-info/

# ad-hoc benchmark output
sift1m_result.txt
